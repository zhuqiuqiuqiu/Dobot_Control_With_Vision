# Role
你是一个精通 Python、PyQt/Tkinter GUI 开发以及工业机器人控制的资深软件工程师。

# Task
我有一个使用 Python 控制越疆（Dobot）MG400 机器人的项目。我将为你提供一个老项目的核心文件 `err_mainC`。

我需要你帮我写一套**全新的机器人控制代码**,用新的名字： new_main_code。

# Requirements
1. **保留与借鉴：** 
   - 提取并保留 `err_mainC` 中关于 **GUI 界面（用户交互界面）** 的布局、组件和逻辑。
   - 提取并保留里面已经写好的 **机器人业务控制功能**（例如特定的运动轨迹、坐标转换、点位逻辑等）。

2. **重构与更新：**
   - `err_mainC` 里面的底层控制和 API 调用使用的是老旧/弃用的方法。在新代码中，请帮我**重构底层的控制和调用逻辑**，使其符合当前标准的、规范的 Python 控制 MG400 的方式（清晰、模块化、易于维护）。
   - 保持 GUI 界面上的按钮、输入框等组件与新的控制逻辑完美绑定。

3. **代码质量：**
   - 代码结构要清晰，将 GUI 界面逻辑与机器人底层控制逻辑尽量解耦（分模块或分流处理）。
   - 包含必要的异常处理（如机器人连接失败、运动异常等）和关键代码注释。

